# .llm/work contains Structural Evidence work artifacts:
# generated views, human declarations, and recorded command results.
# These files are intentionally not committed. Closed evidence records, if a
# project chooses to keep them, belong under .llm/evidence/closed/.
*
!.gitignore
