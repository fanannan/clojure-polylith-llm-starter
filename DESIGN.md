# DESIGN.md — プロダクト仕様

本ファイルは**このプロジェクトで何を作るか**を定義する。
「分類管理の原則」（`.llm/guide/MAINTAINERS_GUIDE.md` §4）で定義される**仕様（DESIGN）**の実装であり、プロジェクトの合意の起点である。

**本ファイルは「現在の仕様」の一次情報源**であり、常に現在形で書かれる。変更履歴は本ファイル内に残さず、ADR（判断経緯）・git（差分）・QUESTIONS アーカイブで保全される。改訂時は該当節を全面書き換えする。

| 項目 | 内容 |
|---|---|
| **対象** | 人間と LLM の仕様協働 |
| **使うタイミング** | 初期化時、仕様変更時 |
| **正本性** | プロダクト仕様の正本 |
| **進めない条件** | §1/§2/§3/§4/§8 が未記入のまま実装へ進まない |

**最小着手条件**: §1 目的 / §2 スコープ / §3 主要ユースケース / §4 受入基準 / §8 プロジェクト固有情報の骨格が埋まれば、初期化を次へ進めてよい。

---

## 0. 本ファイルの埋め方

本節は**人間と LLM が協働で本ファイルを埋めるための作業ガイド**である。初期化時および継続的な改訂時の両方に適用される。

### 0.0 IDEA.md との関係

本ファイルは仕様正本である。任意の着想メモは、本ファイルへ構造化するための入力補助として扱う。
着想から本ファイルへ翻案する時は、仕様翻案ガイドに従う。
¤ .llm/guide/SPEC_GUIDE.md

| 状態 | 扱い |
|---|---|
| `IDEA.md` が存在しない | 従来通り、本ファイルを直接埋める |
| `IDEA.md` が雛形だけ | 着想入力なしとしてスキップする |
| `IDEA.md` に実内容がある | LLM が reconciliation table と本ファイルへの反映案、矛盾、質問候補へ分解する |
| `IDEA.md` と本ファイルが食い違う | 実装判断では本ファイルを優先し、意図不明な差分のみ質問する |

本ファイルが更新されたら、DESIGN 由来の中間表現を再生成し、既存の分析情報 EDN と照合する。分析情報 EDN は実装側の観測済み index であり、仕様正本ではない。

| 生成・照合対象 | 役割 |
|---|---|
| `design-ir.edn` | 本ファイルから抽出した requirement / use case / constraint / test obligation |
| `brick-map.edn` | brick の capability ownership / entrypoint / requirement 対応 |
| `workspace-map.edn` | project / base / component の束ね方と deploy intent |
| `libs.edn` | 用途別機能カテゴリと推奨・非推奨ライブラリ |

### 0.1 セクション区分（凡例）

- 🔴 **必須**: 空白だと LLM は実装判断ができない。初期化完了の条件
- 🟡 **推奨**: プロダクト特性による。空白でも進行可能だが、後で必ず必要になる
- ⚪ **任意**: 必要に応じて追記

### 0.2 埋める順序と担当

**LLM が承認なしに本ファイルを仕様確定として扱うことは禁止**。ただし、着想メモや人間の自由記載から反映案を作ることは推奨する。
∵ .llm/guide/COLLABORATION_GUIDE.md §2.3

以下の順序で、**人間の意思決定または着想入力 → LLM による構造化・文章化 → 人間の最終承認**のサイクルで進める：

| 順序 | セクション | 区分 | 主な担当 | LLM の役割 |
|---|---|---|---|---|
| 1 | §1 目的 | 🔴 | 人間が 1〜2 行の骨子を示す | LLM が文章化して提案 |
| 2 | §2 スコープ / スコープ外 | 🔴 | 人間が「含む / 含まない」を列挙 | LLM が境界の曖昧性を指摘 |
| 3 | §3 主要ユースケース | 🔴 | 人間が 3〜5 個の UC を列挙 | LLM が actor / 前提 / 操作 / 結果 / 例外の 5 要素を補完提案 |
| 4 | §4 受入基準 | 🔴 | 人間が「完成の条件」を示す | LLM が §3 との整合性を確認、テストケース対応を提案（§4.1） |
| 5 | §8 プロジェクト固有情報 | 🔴 | 人間が識別情報・配布形態を決定 | LLM が `workspace.edn` 等の関連ファイルに反映 |
| 6 | §5 利害関係者 | 🟡 | 人間が列挙 | LLM が文章化 |
| 7 | §6 非機能要件 | 🟡 | 人間が数値・方針を決定 | LLM が「高速」等の曖昧表現を具体値で確認 |
| 8 | §7 外部インターフェース | 🟡 | 人間が連携先を列挙 | LLM が詳細項目（認証・SLA・データ形式）を質問 |
| 9 | §9 技術的制約 / §10 将来計画 | ⚪ | 必要時のみ | LLM が該当時に提案 |

> **🔴 必須セクション未記入時の扱い**: §1/§2/§3/§4/§8 が空欄のまま実装に着手することは `CLAUDE.md` §2 禁止事項（仕様欠落による実装判断不可）に該当する。LLM は初期化未完了状態と判定し、人間の意思決定待ちに入る（承認なしで仕様を創作してはならない）。
> ¤ .llm/guide/BOOTSTRAP_GUIDE.md

初期化時は、上の 5 節の骨格が埋まった時点を最短ゴールとして扱う。推奨節・任意節は後続ターンで段階的に埋めてよい。

### 0.3 LLM による仕様曖昧性の能動的指摘

LLM は本ファイルを読むたびに、以下の観点で**能動的に曖昧性を探し**、発見したら**質問してから進める**：
∵ .llm/guide/COLLABORATION_GUIDE.md §4

- **用語定義の曖昧さ**（業務用語の具体的定義）
- **例外条件の欠落**（異常系・境界ケース）
- **数値基準の不明確さ**（「高速」「十分な精度」等）
- **境界条件の不明**（複数ドメインの責務分担）
- **受入基準との不一致**（§4 と §3 の齟齬）
- **既存 KNOWLEDGE との矛盾**

**質問は「1点ずつ確認」原則**（一度に複数まとめない）。
∵ .llm/guide/COLLABORATION_GUIDE.md §4.3

### 0.4 仕様成熟の 3 段階

本ファイルは時間とともに以下の段階を経る。段階ごとに本ファイルの扱い・LLM の姿勢が異なる：

| 段階 | 状態 | 本ファイルの扱い | LLM の姿勢 |
|---|---|---|---|
| **初期仕様**（初期化期） | 必須項目の骨格が埋まった直後、粗い | 実装判断のたびに曖昧性が露呈 | 能動的に曖昧性を指摘、QUESTIONS で議論、確定事項は本ファイルに反映 |
| **成熟期仕様**（実装活発期） | 実装が進み、KNOWLEDGE に契約・不変条件が蓄積 | 運用規約は KNOWLEDGE、決定経緯は ADR に吸収される | KNOWLEDGE / ADR への振り分けを提案、本ファイルは骨格に集中 |
| **安定期仕様**（保守運用期） | 仕様の大部分が実装・運用で固定化 | 本ファイルは骨格のみ、大きな方針転換時のみ改訂 | 日常変更は KNOWLEDGE / ADR へ誘導、本ファイル改訂は ADR 発行と対で提案 |

### 0.5 改訂時のルール

本ファイルは**現在の仕様**を示す一次情報源であり、**変更履歴は本ファイル内に残さない**（分類管理の原則による 4 種文書分離、疲労最小化原則）。変更の記録は以下の 3 箇所で保全される：

- **判断の経緯・根拠**: `.llm/memory/adr/NNNN-topic.md`（不変、supersede で改訂）
- **ファイルレベルの差分**: git コミット履歴
- **変更過程の未決事項**: `.llm/memory/QUESTIONS.md`（未対応(open) → 解決済み(resolved)）

これらが揃っているため、本ファイル内に追記・差分表記・変更履歴節を持つ必要はない。本ファイルは常に**現在形で上書き更新**する（KNOWLEDGE.md §0.5 と同じ運用）。

改訂時の具体的ルール：

- **LLM 独断編集禁止**。改訂はユーザ承認必須（`COLLABORATION_GUIDE.md` §2.3、権限は承認必須 (L1)）
- 大きな方針変更は**ADR を先に発行**してから本ファイルを改訂（詳細は `.llm/memory/adr/README.md`）
- 改訂時は該当節を**全面書き換え**し、古い記述は残さない（差分表示・追記形式にしない）
- コミットメッセージに ADR 番号を含める（例: `Revise DESIGN.md §3 for streaming inference (ADR-0012)`）
- 成熟期以降は、KNOWLEDGE.md / ADR に吸収可能な内容は**本ファイルから移す**ことを検討

変更の流れは CLAUDE の仕様変更フローに従う。
¤ CLAUDE.md §8.5

### 0.6 Requirement ID の定義形式

`brick.edn` / `project.edn` の `:brick/requirements` / `:project/requirements` から参照する ID は、本ファイル内で明示的に定義する。

定義として扱う形式:

```markdown
- REQ-001: 請求書を作成できる
### REQ-002: 請求書を承認できる
```

機械検査は、行頭の bullet または heading に現れる `PREFIX-001` 形式だけを requirement 定義として扱う。文中参照、例示、範囲表記は定義として数えない。

```markdown
- [ ] REQ-001 が動作する       # 参照。定義ではない
- [ ] UC-1 〜 UC-5 が動作する  # 範囲表記。定義ではない
```

推奨:

- Prefix は対象領域を表す短い英大文字にする。例: `INV-001`, `API-001`, `AUTH-001`
- 同じ ID を複数箇所で定義しない
- brick / project に紐づく要求は、実装前に対応する `brick.edn` / `project.edn` に ID を記録する

---

---

## 🔴 1. 目的

<!-- 誰の、どの問題を、なぜ解くのか。1〜3 段落。

     悪い例: 「便利な Web サービスを作る」
     良い例: 「中小企業の経理担当者が月次決算にかかる作業時間を半減させる。
             現状 Excel での手作業が中心で、仕訳ミスが年間数十件発生している」 -->

＜TODO: 埋める＞

---

## 🔴 2. スコープとスコープ外

### 2.1 スコープ

<!-- このプロジェクトで**作る**機能・範囲を列挙 -->

＜TODO: 埋める＞

### 2.2 スコープ外

<!-- このプロジェクトで**作らない**もの。境界を明示しないと LLM が暴走する。
     例：「多通貨対応は含めない（将来課題）」
         「ユーザ認証は含めない（既存の IdP を利用）」 -->

＜TODO: 埋める＞

---

## 🔴 3. 主要ユースケース

<!-- 代表的な 3〜5 個のユースケース。actor → 操作 → 期待される結果 の形式で。
     例：
     ### UC-1: 月次決算の締め処理
     - **actor**: 経理担当者
     - **前提**: 当月の仕訳が全て入力済み
     - **操作**: 「月次締め」ボタンを押下
     - **結果**: 試算表・貸借対照表・損益計算書が PDF で出力される
     - **例外**: 未承認仕訳があればエラー、該当仕訳を一覧表示 -->

＜TODO: 埋める＞

---

## 🔴 4. 受入基準（完成の定義）

<!-- このプロジェクトが「完成した」と言える条件。
     具体的な機能チェックリスト、性能目標、データ整合性など。
     これは LLM が「機能の実装が終わった」と判断する基準にもなる。
     例：
     - [ ] AC-001: [UC-1][REQ-001] 経理担当者が承認済み仕訳を対象に月次締めを実行したら、試算表 PDF が生成される。検証方法: integration test
     - [ ] AC-002: [UC-1][NFR-001] 月次締め処理が 1000 仕訳に対して 10 秒以内に完了する。検証方法: benchmark
     - [ ] 仕訳整合性違反時にデータ破損が発生しない -->

＜TODO: 埋める＞

### 4.1 テストケースとの対応

**各受入基準は、原則 1 つ以上のテストケースに対応させる**。受入基準が具体的に検証可能でないと実装の完了判定ができない。
長期的に追跡する受入基準は、`AC-001: ...` のような明示 ID を付ける。明示 ID がない checklist item も test obligation には変換されるが、文言変更で ID が変わりうる。関連する `UC` / `REQ` が分かる場合は `[UC-1][REQ-001]` のように併記する。
受入基準の推奨形、関連 `UC` / `REQ` の併記、test obligation への変換規則は仕様翻案ガイドに従う。
¤ .llm/guide/SPEC_GUIDE.md

| 受入基準 | 対応テスト | 配置 |
|---|---|---|
| 機能的な基準（UC-N の動作） | interface-test + プロパティテスト | `components/<name>/test/.../interface_test.clj` |
| 性能基準（X 件を Y 秒以内） | ベンチマークテスト | `bases/<name>/test/benchmarks/` |
| データ整合性基準 | プロパティテスト（`test.check`）で不変条件を検証 | `components/<name>/test/.../interface_test.clj` |
| セキュリティ・非機能基準 | 統合テスト + 運用チェックリスト | `bases/<name>/test/integration/` + KNOWLEDGE.md §4 |

受入基準が曖昧でテストに落とせない場合は、`.llm/memory/QUESTIONS.md` に Q を立てて明示化を求める（CLAUDE.md §8.0 仕様曖昧性点検）。

---

## 🟡 5. 利害関係者

<!-- ユーザ、ステークホルダー、運用チーム等の一覧。
     LLM が「誰向けの機能か」を判断する材料になる。 -->

＜TODO: 必要に応じて埋める＞

---

## 🟡 6. 非機能要件

### 6.1 性能
<!-- スループット、レイテンシ、同時接続数等。具体数値で -->

### 6.2 可用性
<!-- 許容ダウンタイム、障害時の復旧目標等 -->

### 6.3 セキュリティ・プライバシー
<!-- 認証認可、PII 取扱い、ログへの機密情報出力禁止等 -->

### 6.4 コンプライアンス
<!-- GDPR、個人情報保護法、業界規制等 -->

＜TODO: 該当するものを埋める＞

---

## 🟡 7. 外部インターフェース・連携先

<!-- 連携する外部システム・API・DB の一覧。
     URL、認証方式、データ形式、SLA 等。
     連携コンポーネントの実装判断に直結する。 -->

＜TODO: 該当するものを埋める＞

---

## 🔴 8. プロジェクト固有情報

<!-- このプロジェクトの基本属性。初期化時に埋める。
     CLAUDE.md や workspace.edn の TODO と対応する。 -->

### 8.1 識別情報

- **プロジェクト名**: ＜TODO＞
- **トップ名前空間**: ＜TODO: `myorg.myapp` から実名に変更、`workspace.edn` の `:top-namespace` と一致＞
- **組織・所有者**: ＜TODO＞

### 8.2 ランタイム

- **JVM**: 21 LTS
- **Clojure**: 1.12

<!-- 変更する場合は ADR を発行して理由を記録 -->

### 8.3 技術選定

本プロジェクトで必須技術基盤以外に採用する技術を、用途別機能カテゴリ単位で記録する。必要になったものだけを書く。

- **エントリ種別**: ＜TODO: Web API / CLI / バッチ / ライブラリ / ワーカ / bot / GUI 等＞
- **追加する用途別機能カテゴリ**: ＜TODO: HTTP / 永続化 / ジョブ実行 / 外部 API クライアント / 開発支援 等＞
- **採用ライブラリ**: ＜TODO: 用途別機能カテゴリごとに coord と用途を記録＞
- **推奨カタログからの逸脱**: ＜TODO: なし / あり（ADR-NNNN 参照）＞

### 8.4 配布形態

- **形式**: ＜TODO: 単一 uberjar / 複数 uberjar / Docker イメージ / Lambda / ライブラリ JAR 等＞
- **Polylith project / deploy 名**: ＜TODO: 例 `app`、`worker`、`api`。`projects/<deploy>` と `workspace.edn :projects` に対応＞
- **ビルドコマンド**: ＜TODO: 例 `cd projects/<deploy>/ && clj -T:build uber`＞
- **デプロイ先**: ＜TODO: 例 AWS ECS / GCP Cloud Run / オンプレ等＞

#### 8.4.1 deploy project 構造

deploy 名ごとに、`projects/<deploy>/` に置くファイルと出力をここに仕様として確定する。この欄が埋まれば、`projects/<deploy>/` の作成は新規 project の架構判断ではなく合意済み仕様の実装になる（`poly create project` の実行と、project.edn / deps.edn / build.clj の記述）。

- **`projects/<deploy>/project.edn`**: ＜TODO: `:project/type`（`:app` / `:library`）、`:project/runtime`、`:project/entrypoints`、`:project/includes`（bases / components）、`:project/build`＞
- **`projects/<deploy>/deps.edn`**: ＜TODO: include する base / component を `:local/root` で列挙＞
- **`projects/<deploy>/build.clj`**: ＜TODO: uberjar 配布時のみ。`lib` 名・`main-ns`・`version`＞
- **ビルド成果物の出力パス**: ＜TODO: 例 `projects/<deploy>/target/<deploy>-standalone.jar`＞
- **起動コマンド**: ＜TODO: 例 `java -jar projects/<deploy>/target/<deploy>-standalone.jar`。起動 smoke の入口になる＞

### 8.5 アーキテクチャ構造

- **Polylith**（components / bases / projects / development）
- **Brick Map**: brick 構成・機能分担・公開 API は閲覧用生成物と `.llm/data/brick-map.edn`（検索用生成物）で確認する。正本は各 `brick.edn` と `interface.clj`
- **Project / Workspace Map**: deploy 単位と workspace 全体の構成は閲覧用生成物と `.llm/data/workspace-map.edn`（検索用生成物）で確認する。正本は `project.edn`、`workspace.edn`、`deps.edn`、`brick.edn`
- 構造詳細は別紙に置く
∵ .llm/guide/POLYLITH_GUIDE.md

---

## ⚪ 9. 技術的制約

<!-- 技術選定に影響する外部制約。
     例：「既存 DB スキーマを変更不可」
         「デプロイ先は AWS ECS 固定」
         「レガシーシステムとの互換性維持が必要」 -->

＜TODO: 必要に応じて埋める＞

---

## ⚪ 10. 将来計画

<!-- 現バージョンでは作らないが将来検討する機能。
     §2.2 スコープ外との違い: スコープ外は「作らない」、将来計画は「後で作るかも」。
     LLM がスコープ外を「実装すべき」と誤認するのを防ぐ補助情報。 -->

＜TODO: 必要に応じて埋める＞
