# DESIGN.md — プロダクト仕様

本ファイルは**このプロジェクトで何を作るか**を定義する。
原則 13（`project-guide/MAINTAINERS_GUIDE.md` §4）で定義される**仕様（DESIGN）**の実装であり、プロジェクトの合意の起点である。

**本ファイルは「現在の仕様」の一次情報源**であり、常に現在形で書かれる。変更履歴は本ファイル内に残さず、ADR（判断経緯）・git（差分）・QUESTIONS.md（未決事項のアーカイブ）で保全される。改訂時は該当節を全面書き換えする（§0.5 参照）。

---

## 0. 本ファイルの埋め方

本節は**人間と LLM が協働で本ファイルを埋めるための作業ガイド**である。ブートストラップ時および継続的な改訂時の両方に適用される。

### 0.1 セクション区分（凡例）

- 🔴 **必須**: 空白だと LLM は実装判断ができない。ブートストラップ完了の条件
- 🟡 **推奨**: プロダクト特性による。空白でも進行可能だが、後で必ず必要になる
- ⚪ **任意**: 必要に応じて追記

### 0.2 埋める順序と担当

**LLM が独断で本ファイルを編集することは禁止**（`project-guide/COLLABORATION_GUIDE.md` §2.3）。
以下の順序で、**人間の意思決定 → LLM による文章化・整形 → 人間の最終承認**のサイクルで進める：

| 順序 | セクション | 区分 | 主な担当 | LLM の役割 |
|---|---|---|---|---|
| 1 | §1 目的 | 🔴 | 人間が 1〜2 行の骨子を示す | LLM が文章化して提案 |
| 2 | §2 スコープ / スコープ外 | 🔴 | 人間が「含む / 含まない」を列挙 | LLM が境界の曖昧性を指摘 |
| 3 | §3 主要ユースケース | 🔴 | 人間が 3〜5 個の UC を列挙 | LLM が actor / 前提 / 操作 / 結果 / 例外の 5 要素を補完提案 |
| 4 | §4 受入基準 | 🔴 | 人間が「完成の条件」を示す | LLM が §3 との整合性を確認、テストケース対応を提案（§4.1） |
| 5 | §8 プロジェクト固有情報 | 🔴 | 人間が識別情報・配布形態を決定 | LLM が `workspace.edn` 等の関連ファイルに反映 |
| 6 | §5 利害関係者 | 🟡 | 人間が列挙 | LLM が文章化 |
| 7 | §6 非機能要件 | 🟡 | 人間が数値・方針を決定 | LLM が「高速」等の曖昧表現を具体値で確認 |
| 8 | §7 外部インターフェース | 🟡 | 人間が連携先を列挙 | LLM が詳細項目（認証・SLA・データ形式）を質問 |
| 9 | §9 技術的制約 / §10 将来計画 | ⚪ | 必要時のみ | LLM が該当時に提案 |

> **🔴 必須セクション未記入時の扱い**: §1/§2/§3/§4/§8 が空欄のまま実装に着手することは `CLAUDE.md` §2 禁止事項（仕様欠落による実装判断不可）に該当する。LLM はブートストラップ未完了状態と判定し、`BOOTSTRAP_GUIDE.md` を参照して人間の意思決定待ちに入る（承認なしで仕様を創作してはならない）。

### 0.3 LLM による仕様曖昧性の能動的指摘

LLM は本ファイルを読むたびに、以下の観点で**能動的に曖昧性を探し**、発見したら**質問してから進める**（詳細は `project-guide/COLLABORATION_GUIDE.md` §4）：

- **用語定義の曖昧さ**（業務用語の具体的定義）
- **例外条件の欠落**（異常系・境界ケース）
- **数値基準の不明確さ**（「高速」「十分な精度」等）
- **境界条件の不明**（複数ドメインの責務分担）
- **受入基準との不一致**（§4 と §3 の齟齬）
- **既存 KNOWLEDGE との矛盾**

**質問は ONE BY ONE 原則**（一度に複数まとめない、`COLLABORATION_GUIDE.md` §4.3）。

### 0.4 仕様成熟の 3 段階

本ファイルは時間とともに以下の段階を経る。段階ごとに本ファイルの扱い・LLM の姿勢が異なる：

| 段階 | 状態 | 本ファイルの扱い | LLM の姿勢 |
|---|---|---|---|
| **初期仕様**（ブートストラップ期） | 必須項目の骨格が埋まった直後、粗い | 実装判断のたびに曖昧性が露呈 | 能動的に曖昧性を指摘、QUESTIONS で議論、確定事項は本ファイルに反映 |
| **成熟期仕様**（実装活発期） | 実装が進み、KNOWLEDGE に契約・不変条件が蓄積 | 運用規約は KNOWLEDGE、決定経緯は ADR に吸収される | KNOWLEDGE / ADR への振り分けを提案、本ファイルは骨格に集中 |
| **安定期仕様**（保守運用期） | 仕様の大部分が実装・運用で固定化 | 本ファイルは骨格のみ、大きな方針転換時のみ改訂 | 日常変更は KNOWLEDGE / ADR へ誘導、本ファイル改訂は ADR 発行と対で提案 |

### 0.5 改訂時のルール

本ファイルは**現在の仕様**を示す一次情報源であり、**変更履歴は本ファイル内に残さない**（原則 13 の 4 種文書分離、疲労最小化原則）。変更の記録は以下の 3 箇所で保全される：

- **判断の経緯・根拠**: `project-memory/adr/NNNN-topic.md`（不変、supersede で改訂）
- **ファイルレベルの差分**: git コミット履歴
- **変更過程の未決事項**: `project-memory/QUESTIONS.md`（open → resolved）

これらが揃っているため、本ファイル内に追記・差分表記・変更履歴節を持つ必要はない。本ファイルは常に**現在形で上書き更新**する（KNOWLEDGE.md §0.5 と同じ運用）。

改訂時の具体的ルール：

- **LLM 独断編集禁止**。改訂はユーザ承認必須（`COLLABORATION_GUIDE.md` §2.3、権限 L1）
- 大きな方針変更は**ADR を先に発行**してから本ファイルを改訂（詳細は `project-memory/adr/README.md`）
- 改訂時は該当節を**全面書き換え**し、古い記述は残さない（差分表示・追記形式にしない）
- コミットメッセージに ADR 番号を含める（例: `Revise DESIGN.md §3 for streaming inference (ADR-0012)`）
- 成熟期以降は、KNOWLEDGE.md / ADR に吸収可能な内容は**本ファイルから移す**ことを検討

変更の流れは `CLAUDE.md` §8.5 仕様変更・追加への対処 を参照。

---

---

## 🔴 1. 目的

<!-- 誰の、どの問題を、なぜ解くのか。1〜3 段落。

     悪い例: 「便利な Web サービスを作る」
     良い例: 「中小企業の経理担当者が月次決算にかかる作業時間を半減させる。
             現状 Excel での手作業が中心で、仕訳ミスが年間数十件発生している」 -->

＜TODO: 埋める＞

---

## 🔴 2. スコープとスコープ外

### 2.1 スコープ

<!-- このプロジェクトで**作る**機能・範囲を列挙 -->

＜TODO: 埋める＞

### 2.2 スコープ外

<!-- このプロジェクトで**作らない**もの。境界を明示しないと LLM が暴走する。
     例：「多通貨対応は含めない（将来課題）」
         「ユーザ認証は含めない（既存の IdP を利用）」 -->

＜TODO: 埋める＞

---

## 🔴 3. 主要ユースケース

<!-- 代表的な 3〜5 個のユースケース。actor → 操作 → 期待される結果 の形式で。
     例：
     ### UC-1: 月次決算の締め処理
     - **actor**: 経理担当者
     - **前提**: 当月の仕訳が全て入力済み
     - **操作**: 「月次締め」ボタンを押下
     - **結果**: 試算表・貸借対照表・損益計算書が PDF で出力される
     - **例外**: 未承認仕訳があればエラー、該当仕訳を一覧表示 -->

＜TODO: 埋める＞

---

## 🔴 4. 受入基準（完成の定義）

<!-- このプロジェクトが「完成した」と言える条件。
     具体的な機能チェックリスト、性能目標、データ整合性など。
     これは LLM が「機能の実装が終わった」と判断する基準にもなる。
     例：
     - [ ] UC-1 〜 UC-5 が全て動作する
     - [ ] 月次締め処理が 1000 仕訳に対して 10 秒以内に完了する
     - [ ] 仕訳整合性違反時にデータ破損が発生しない -->

＜TODO: 埋める＞

### 4.1 テストケースとの対応

**各受入基準は、原則 1 つ以上のテストケースに対応させる**。受入基準が具体的に検証可能でないと実装の完了判定ができない。

| 受入基準 | 対応テスト | 配置 |
|---|---|---|
| 機能的な基準（UC-N の動作） | interface-test + プロパティテスト | `components/<n>/test/.../interface_test.clj` |
| 性能基準（X 件を Y 秒以内） | ベンチマークテスト | `bases/<n>/test/benchmarks/` |
| データ整合性基準 | プロパティテスト（`test.check`）で不変条件を検証 | `components/<n>/test/.../interface_test.clj` |
| セキュリティ・非機能基準 | 統合テスト + 運用チェックリスト | `bases/<n>/test/integration/` + KNOWLEDGE.md §4 |

受入基準が曖昧でテストに落とせない場合は、`project-memory/QUESTIONS.md` に Q を立てて明示化を求める（CLAUDE.md §8.0 仕様曖昧性点検）。

---

## 🟡 5. 利害関係者

<!-- ユーザ、ステークホルダー、運用チーム等の一覧。
     LLM が「誰向けの機能か」を判断する材料になる。 -->

＜TODO: 必要に応じて埋める＞

---

## 🟡 6. 非機能要件

### 6.1 性能
<!-- スループット、レイテンシ、同時接続数等。具体数値で -->

### 6.2 可用性
<!-- 許容ダウンタイム、障害時の復旧目標等 -->

### 6.3 セキュリティ・プライバシー
<!-- 認証認可、PII 取扱い、ログへの機密情報出力禁止等 -->

### 6.4 コンプライアンス
<!-- GDPR、個人情報保護法、業界規制等 -->

＜TODO: 該当するものを埋める＞

---

## 🟡 7. 外部インターフェース・連携先

<!-- 連携する外部システム・API・DB の一覧。
     URL、認証方式、データ形式、SLA 等。
     連携コンポーネントの実装判断に直結する。 -->

＜TODO: 該当するものを埋める＞

---

## 🔴 8. プロジェクト固有情報

<!-- このプロジェクトの基本属性。ブートストラップ時に埋める。
     CLAUDE.md や workspace.edn の TODO と対応する。 -->

### 8.1 識別情報

- **プロジェクト名**: ＜TODO＞
- **トップ名前空間**: ＜TODO: `myorg.myapp` から実名に変更、`workspace.edn` の `:top-namespace` と一致＞
- **組織・所有者**: ＜TODO＞

### 8.2 ランタイム

- **JVM**: 21 LTS
- **Clojure**: 1.12

<!-- 変更する場合は ADR を発行して理由を記録 -->

### 8.3 採用 stack

本プロジェクトで採用する技術スタック構成を記録する。選定論理と詳細は `project-guide/STACK_GUIDE.md` を参照。

- **主 stack**: ＜TODO: 例 web-api stack、batch stack、library stack 等＞
- **補助 stack**: ＜TODO: なし / batch stack 等（複数目的を持つプロジェクトの場合）＞
- **横断 stack**: ＜TODO: dev-tools stack（強く推奨）＞
- **STACK_GUIDE.md 推奨からの逸脱**: ＜TODO: なし / あり（ADR-NNNN 参照）＞
- **プロジェクト固有の追加ライブラリ**: ＜TODO: 例 `org.postgresql/postgresql`（DB ドライバ）、`com.cognitect.aws/sqs`（AWS SQS クライアント）等＞

### 8.4 配布形態

- **形式**: ＜TODO: 単一 uberjar / 複数 uberjar / Docker イメージ / Lambda / ライブラリ JAR 等＞
- **ビルドコマンド**: ＜TODO: 例 `cd projects/<deploy>/ && clj -T:build uber`＞
- **デプロイ先**: ＜TODO: 例 AWS ECS / GCP Cloud Run / オンプレ等＞

### 8.5 アーキテクチャ構造

- **Polylith**（components / bases / projects / development）
- 構造詳細は `project-guide/POLYLITH_GUIDE.md` を参照

---

## ⚪ 9. 技術的制約

<!-- 技術選定に影響する外部制約。
     例：「既存 DB スキーマを変更不可」
         「デプロイ先は AWS ECS 固定」
         「レガシーシステムとの互換性維持が必要」 -->

＜TODO: 必要に応じて埋める＞

---

## ⚪ 10. 将来計画

<!-- 現バージョンでは作らないが将来検討する機能。
     §2.2 スコープ外との違い: スコープ外は「作らない」、将来計画は「後で作るかも」。
     LLM がスコープ外を「実装すべき」と誤認するのを防ぐ補助情報。 -->

＜TODO: 必要に応じて埋める＞
