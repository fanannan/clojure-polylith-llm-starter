# _POSSIBLE_ISSUES.md — 第一原理で再判定した論点集

本ファイルは過去セッションで累積した 310 件超の指摘を、**CLAUDE.md §1 疲労最小化原理**に照らして再整理したもの。
原理の観点から見て不適切な指摘（「検証なし」を一律問題視する類）は**無視対象**として別扱いにする。

## 実装ステータス（2026-04 時点）

- **完了**: A-1〜A-18（A 系全件、A-8 と A-13 は近似実装、A-19 は保留確定）、B-1〜B-4、C-1〜C-3、D-1〜D-6、F-1 / F-3、F 拡張（Splint / clj-watson 必須層化、clj-kondo linter 38 追加・昇格）、G-1 / G-2 / G-3、§4 相互参照点検（後続タスクとして記録）
- **未着手**: E-1（本プラン ADR 発行、ユーザー指示で除外）、E-2（前セッションの完了条件未実行、除外）、F-2 / F-4（保留確定）、A-9 / A-12 の `:ns-groups` 活性化（brick 名確定後の bootstrap タスク）
- **既知の制限**: A-8 の defn-vs-m/=> アリティ突合は `scripts/check-interface-contracts.sh`（file-level）で補完、A-13 は最小実装（厳密な戻り値未使用検知は parent-node API 拡張待ち）、A-17 destructuring 深さは `:keys`/`:strs`/`:syms` 系を除外する簡易実装で閾値 `> 3`（A-17 の本来「深さ 2 超過」より緩い、false positive 回避のため）

---

## 0. 判定基準（CLAUDE.md からの引用）

| 基準 | 出典 | 適用 |
|---|---|---|
| **§1.2.1 機械化** | ツールで強制する、人間注意力に頼らない | 「規約はあるがツール未設定」は真の問題 |
| **§1.2.5 承認は不可逆性の根拠でのみ置く** | 「念のため」は原則違反 | 「念のため記録/承認」を積む提案は棄却 |
| **§1.3 規約で縛れば守られるは誤り** | 守る手段を同時に設計 | 守る手段がない規約は「機械化 or 廃止」の二択 |
| **§1.2.4 早期破棄・§1.2.5 早期検知** | 完遂にこだわらない | 累積した儀式・カウント運用は疲労源として切る |
| **§11 4 種文書の役割分離** | DESIGN / KNOWLEDGE / ADR / QUESTIONS の対照性 | 役割の違う文書間の「重複」は重複ではない |

**判定ルール**:
1. 規約だけで守る手段がない → **A. 機械化 or 廃止を迫る**
2. 不可逆性の根拠なき承認・記録 → **B. 儀式削減を迫る**
3. 設定・文書間の矛盾 → **C. 解消を迫る**
4. ブートストラップ直後の UX 失敗 → **D. 初期状態の健全化を迫る**
5. 「判断は本質的に主観」と「検証不能」を同視している → **無視（原理誤読）**
6. 未読領域からの推測／本セッション運用の雑事 → **無視（実体なし）**

---

## Part 1. 有効な論点（原理に照らして対処すべき）

### A. §1.3 違反: 規約あり／機械化手段なし

どれも **clj-kondo 設定の追加**または **規約の廃止**の二択で閉じる。どちらかに倒せば片が付く。

#### A-1. `m/=>` 契約の全公開関数付与 【優先度: 高・即採用】

- **規約**: CLAUDE.md §4.1、CODING_GUIDE.md §2.1.1（`interface.clj` の公開関数は必須）
- **現状**: `.clj-kondo/config.edn` の `:lint-as {malli.core/=> clj-kondo.lint-as/def-catch-all}` は「=> の引数を壊さない」ためで、「=> の有無強制」ではない
- **起きること**: 契約忘れが CI で素通りし、境界検証が欠落した関数が出荷される
- **重要性**: 本テンプレートの最重要原則である **§1.1.1 全域性**の守護機構。機械化なしではこの核原則が §1.3 違反のまま放置され、他の機械化（A-2〜A-6）との一貫性が崩れる。LLM の類型的失敗「書き忘れ」が最も起きやすい領域でもある
- **対処**: `.clj-kondo/hooks/` 配下に custom hook を配置し、`.clj-kondo/config.edn` で登録する。検証内容は「`interface.clj` 配下の各 `defn` について、同じ namespace 内に対応する `m/=>` 宣言があるか」。hook 形態は Malli / Polylith 公式 hook と同じで、本テンプレートは既にこの配布形態を受容している。保守負担は A-6 / A-7 と同程度で、中核原則保護のため優先採用

#### A-2. `:unsorted-required-namespaces` が `:warning`

- **規約**: CODING_GUIDE.md §14.4（`ns` の `:require` はアルファベット順）
- **現状**: `.clj-kondo/config.edn` で `:warning` のまま
- **対処**: `:error` に昇格。1 行の設定変更で機械化完了

#### A-3. CODING §1.9「comment 内実装忘れ禁止」は機械化不可能な規約

- **現状**: `(comment ...)` は Clojure の慣用として **REPL 駆動開発中の試行コード置き場**として広く使われる。意図的に「動かないコード」「仮のコード」を置くのが標準作法。`.clj-kondo/config.edn` の `:skip-comments true` はこの慣用を壊さないための適切な設定
- **誤った対処案（却下）**: `:skip-comments false` にすると、REPL 探索で置いた `(comment (user/find-by-id :nonexistent))` のような意図的試行コードに lint エラーが出る。これは §1.2.2 ループ短縮を阻害し、疲労増
- **実態**: CODING_GUIDE.md §1.9 の「実装を退避したまま戻し忘れる」は意図論の問題で、lint では検知困難な領域
- **対処**: `:skip-comments` は `true` のまま維持。CODING_GUIDE.md §1.9 の該当規約を削除するか、「機械化できない努力目標」と明示する。検知したいなら「commit 前に `git diff` で `(comment` の増減を目視」程度の軽い運用に格下げ

#### A-4. `ex-info` 義務化に検知なし

- **規約**: CODING_GUIDE.md §7.1（`(throw (Exception. ...))` 禁止、`ex-info` 必須）
- **現状**: `clj-kondo` に `:discouraged-var` で `java.lang.Exception` コンストラクタを登録していない
- **対処**: `:discouraged-var` に追加。

#### A-5. `with-redefs` の「最小範囲」基準

- **規約**: CLAUDE.md §4.3、CODING_GUIDE.md §4.3（最小範囲のみ）
- **現状**: `:discouraged-var` で警告は出るが「最小」は判定不能
- **対処**: 規約から「最小範囲」という主観語を外し、「使用時は依存注入化を ADR で理由付け」という検査可能な運用に変える。

#### A-6. STACK_GUIDE.md §8.2 非推奨ライブラリ 17 種の照合機構なし

- **現状**: LLM が提案時に照合する前提だが、`clj-kondo` に登録されていない
- **対処**: timbre / Compojure / friend 等の代表シンボルを `:discouraged-var` に登録。登録できないもの（ライブラリ採用自体）は `:lint-as` で `ns :require` 検知に切り替える

#### A-7. 関数行数上限（20 / 50）の検知なし

- **規約**: CLAUDE.md §1.2.3・CODING_GUIDE.md §4.4
- **現状**: `clj-kondo` 標準に関数行数ルールなし
- **誤った対処案（却下）**: commit hook で計測するアプローチは §8.0.0 ターン内検証に乗らない。編集〜検証を秒単位で閉じる設計（§1.2.2 ループ短縮）に反する
- **対処**: **clj-kondo custom hook** で実装する。`clj-kondo-fn-size` 等のサードパーティ hook 相当を `.clj-kondo/hooks/` に配置。`clj -M:lint` のターン内検証に自動的に組み込まれる。カウント基準は「`defn` 本体の行数、`(comment ...)` と空行を除外」。20 行を「ロジック関数」、50 行を「インターフェース関数」の上限として適用。hook は A-1 と同じ配布形態で受容可能

---

追加レビュー（外形的条件で機械化可能と判定された未機械化規約）を以下に統合する。A-8〜A-12 は **§1.1 三基底原則の守護に直結する**ため優先度高、A-13〜A-18 は実装コストに対する価値が高い中優先、A-19 は低優先として保留・除外項目を整理する。

#### A-8. `m/=>` と `defn` のアリティ整合 【優先度: 高】

- **規約**: Malli 契約の意味的要件（CODING_GUIDE.md §2.1.1 に暗黙含まれる）
- **外形的条件**: `(m/=> foo [:=> [:cat int? int?] int?])` と `(defn foo [a b c])` のように、`[:cat ...]` の要素数と `defn` の引数数が一致しない
- **対処**: clj-kondo custom hook で同一 namespace 内の `m/=>` 宣言と `defn` シグネチャのアリティを突合。型検査は不要、引数数だけ比較するので実装複雑度は中

#### A-9. ドメイン component の top-level `atom` 禁止 【優先度: 高】

- **規約**: CLAUDE.md §4.2、CODING_GUIDE.md §1.5・§8.2（可変状態は最上位層のみ）
- **外形的条件**: `components/<domain>/src/` 配下で `(def foo (atom ...))` / `(defonce foo (atom ...))` の top-level 宣言
- **対処**: `.clj-kondo/config.edn` の `:ns-groups` でドメイン component を括り、`:config-in-ns` で `(def ... (atom ...))` パターンを custom hook または `:discouraged-var` で警告化。`development/src` と `dev/user.clj` は除外

#### A-10. `catch Throwable` / `catch Exception` の広範囲 catch 禁止 【優先度: 高】

- **規約**: CODING_GUIDE.md §7.2（catch 節は限定的に）
- **外形的条件**: `(catch Throwable _ ...)` または `(catch Exception _ ...)` を直接記述
- **対処**: clj-kondo custom hook で catch 節のクラス名を検査。`Throwable` / `Exception` / `java.lang.Exception` / `java.lang.Throwable` を `:error` または `:warning` で報告。回避したい場合は `ex-info` 相当の具体クラスで catch させる

#### A-11. 空 catch（例外の握り潰し）禁止 【優先度: 高】

- **規約**: CODING_GUIDE.md §7.2
- **外形的条件**: `(catch Foo e)` のように本体ゼロ、または `(catch Foo e nil)` のように nil 単独返却
- **対処**: clj-kondo custom hook で catch 本体の形状を検査。本体が空または単一の `nil` / `false` リテラルなら `:error`

#### A-12. ドメイン層での `slurp` / `spit` / `java.io.File.` 禁止 【優先度: 高】

- **規約**: CODING_GUIDE.md §2.3（副作用隔離）、CLAUDE.md §4.3
- **外形的条件**: ドメイン component 配下で `slurp` / `spit` / `java.io.File.` / `clojure.java.io/*` を参照
- **対処**: `.clj-kondo/config.edn` の `:ns-groups` でドメイン component を括り、`:config-in-ns` で該当 I/O 関数を `:discouraged-var`（ドメイン内では `:error`）として登録。雛形は既にコメントアウトで配布済みなので有効化するだけ

#### A-13. `m/validate` の戻り値を捨てるパターンの検知 【優先度: 中】

- **規約**: CODING_GUIDE.md §2.1.2（境界で検証）
- **外形的条件**: `(m/validate schema value)` 単体が `do` / `let` ボディ途中等で戻り値が `if` / `when` / `assert` / スレッディングに使われず破棄されている
- **対処**: clj-kondo custom hook で `m/validate` 呼び出しの評価文脈を検査。`:unused-value` 系のパターン実装

#### A-14. 1 ファイル 1 名前空間 【優先度: 中】

- **規約**: CODING_GUIDE.md §14.1
- **外形的条件**: 1 つの `.clj` ファイル内に複数の `(ns ...)` 宣言
- **対処**: clj-kondo custom hook で `ns` 出現数をカウント。false positive なし、実装複雑度低

#### A-15. 位置引数 4 個以上の禁止 【優先度: 中】

- **規約**: CODING_GUIDE.md §4.2（4 個以上はマップで受ける）
- **外形的条件**: `defn` の引数リストが位置引数で 4 つ以上（destructuring / 可変長 `& args` / マルチアリティ分岐は除外）
- **対処**: clj-kondo custom hook で引数リストを検査。可変長・マルチアリティの判定に注意

#### A-16. `go` ブロック内の `<!!` / `>!!`（ブロッキング）禁止 【優先度: 中】

- **規約**: CODING_GUIDE.md §10.2
- **外形的条件**: `(go ...)` 本体内で `<!!` / `>!!` を呼ぶ
- **対処**: clj-kondo custom hook で `go` 内部のシンボル参照を検査。`slurp` 等の blocking 呼び出しの網羅は困難なので、まず `<!!` / `>!!` だけを確実に検出する

#### A-17. destructuring のネスト 2 超過 【優先度: 中】

- **規約**: CODING_GUIDE.md §1.12
- **外形的条件**: 引数の destructuring ネスト深度が 2 を超える
- **対処**: clj-kondo custom hook でネスト深度をカウント

#### A-18. production コードでの `Thread/sleep` 禁止 【優先度: 中】

- **規約**: 暗黙規約（§1.1.3 副作用隔離の延長）
- **外形的条件**: `components/` / `bases/` 配下で `Thread/sleep` 参照（`development/src` は除外）
- **対処**: `:discouraged-var` に `java.lang.Thread/sleep` を登録、または `:config-in-ns` でドメイン系のみ `:error`

#### A-19. 採用保留・除外（本テンプレートでは追加しない）

以下は検討対象に上がったが、false positive または既存機構でカバー済のため現時点では追加しない。将来実運用で違反が観察された場合に再検討する（§1.2.5 早期検知 > 事前承認の原則）。

- **`loop` / `recur` の `:warning` 化**: 最終手段として正当な使用場面があり false positive が多い。ADR で理由付けを要求する運用で代替
- **ドメインキーの名前空間付与強制**: 境界判定が困難（HTTP リクエスト parsed キー等は素のキーワードが正当）。`interface.clj` のスキーマ定義内に限定すれば可能だが、優先度低
- **`deftest` 命名規約（when-X-then-Y）**: 命名慣用は厳密強制すべきでない領域
- **Integrant 関連検査**（`ig/init-key` と `ig/halt-key!` 対称性、`ig/ref` 解決）: workspace 横断解析が必要で `poly check` の領域
- **Polylith 構造検査**（`ns` とファイルパス対応、interface 以外からの require）: `poly check` で完全カバー済
- **真偽値フラグ引数**: 名前ベース判定は false positive 多、除外
- **`(comment ...)` 内実装忘れ**: A-3 で議論済、Clojure 慣用と衝突するため保留
- **`deps.edn` 直接変更**: clj-kondo では検出困難。pre-commit hook または CI 差分検査の領域
- **テストファイル配置（`interface_test.clj` 以外）**: Polylith のファイル配置規約の領域

### B. §1.2.5 違反: 不可逆性の根拠なき儀式

どれも「儀式を廃止するか、残すなら機械化」の判断が必要。残存コストと再利用価値を比較する。

#### B-1. `§7.2` 進捗メモ（毎ターン必須）→ 「必要時のみ」に緩和

- **現状**: 毎ターン「目標 / 今回の試行 / 前ターンからの変化 / 同一問題の連続試行 N 回目」を書く規約
- **全廃止案が行き過ぎな理由**:
  - **context 圧縮への備え**: 長い conversation で自動 compaction が発生した場合、外形化したメモが LLM の自己状態復元材料になる。実際、本プロジェクトでは stage 間で compaction が発生している
  - **ユーザの確認材料**: 「3 回試行したと LLM は認識しているか」をユーザが見る手段。LLM の自己認識を外部から監視する低コストな窓
- **問題の本質**: 「毎ターン必須」という**頻度**が過剰。価値を生まない平常ターンにも儀式コストを払う
- **対処**: §7.2 を残しつつ、発動条件を**「必要時のみ」**に緩和する。具体的には:
  - 自己停止条件（§7.1）の閾値に近づいた時（2 回目の同一試行から）
  - 同じファイルへの編集が 3 回を超えた時
  - 仮説と検証のループに入った時
  - 平常ターン（1 回で完結する編集・単純な情報取得）は免除
- **実装**: §7.2 の「コード編集を伴う全ターンで必須」を「自己停止条件接近時または困難状況下で必須」に書き換える

#### B-2. ADR の supersede チェイン

- **現状**: 「旧 ADR の status を superseded-by-N に、新 ADR の Related に Supersedes: N-old を」という双方向更新。遡読者は実質不在
- **廃止根拠**: 最新 accepted ADR だけが生きた情報。チェイン整合のコストに価値が伴っていない
- **対処**: 双方向更新規約を片方向（新 ADR のみ）に簡素化。または「旧 ADR の Status は触らない、新 ADR で上書き宣言する」運用に変える

#### B-3. `QUESTIONS.md` resolved の §3 アーカイブ → 昇格先別に選別削除

- **現状**: resolved にした Q をすべて §3 に移動、100 件超で年次ファイル分離
- **完全削除が行き過ぎな理由**:
  - **ADR 昇格 Q**: 判断過程（選択肢・利点欠点）は ADR 本体（Context / Considered Alternatives）に保全される。Q の残存は冗長なので削除可
  - **DESIGN.md / KNOWLEDGE.md 昇格 Q**: 昇格先には「結論」しか書かれず、**「なぜその結論になったか」「どの選択肢を検討したか」は保全されない**。ここで Q を削除すると判断過程が git log にしか残らず、遡読コストが跳ね上がる
  - **wontfix / superseded Q**: 結論も残らないため、アーカイブする価値がある
- **対処（選別削除）**:
  - **削除**: ADR 昇格 Q（軌跡は ADR 側で完全保全される）
  - **§3 に軽く残す**: DESIGN / KNOWLEDGE 昇格 Q、wontfix / superseded Q（Q-ID・結論・昇格先の 3 行要約のみ、詳細は git log 参照）
  - **廃止**: 「100 件超で年次分離」規約（儀式コスト、到達する前に §3 軽量化で吸収）

#### B-4. `malli-off!` ヘルパー

- **現状**: 停止すべき場面（計測時等）が未文書化。誤って off にされると契約違反が見逃される
- **対処**: `malli-off!` を dev/user.clj から削除し、必要時は `mi/unstrument!` を直接呼ぶ（用途を明示的にドキュメント化する手間を負う運用）

### C. 文書／設定間の矛盾・相互参照の未構築

「一箇所に縮退」で片付く単純重複と、**上流・下流の参照関係が未構築**な構造欠落を区別して扱う。

#### C-1. `:skip-comments true` と CODING §1.9 の矛盾

A-3 と同一問題。A-3 で処理される

#### C-2. 必須層・stack 層・横断層の説明が 3 箇所に tiered で存在

- **判定**: 3 箇所は異なる使用シーン向けの意図的 tiered structure（日常参照 / 権威仕様 / 初期化導線）。縮退は §1.2.2 ループ短縮と逆行するため不採用
- **3 箇所の実態**:
  - `CLAUDE.md §3`: LLM 日常参照の常時制約、必須層 6 項目、version 省略（概念層）
  - `STACK_GUIDE.md §2`: 選定論理の正本、必須層 10 項目、version 明記（権威仕様）
  - `BOOTSTRAP_GUIDE.md §1`: 初期化導線、必須層 6 項目、STACK_GUIDE に委譲（手順層）
- **実在する問題**: version 記載粒度の不均衡と、必須層列挙の項目数差（6 vs 10）— これは意図的な抽象度差でもある。問題は **version 更新時の drift リスク**
- **対処**:
  - `STACK_GUIDE.md §2` を**正本として本文で明示**する
  - `CLAUDE.md §3` と `BOOTSTRAP_GUIDE.md §1` は**概念的な必須層列挙のみ**に留め、version は書かない運用を維持（現状どおり、明文化で保全）
  - `MAINTAINERS_GUIDE.md §5.1`（依存更新手順）に「必須層 version 更新時は `STACK_GUIDE.md §2` のみ更新、`CLAUDE.md §3` / `BOOTSTRAP §1` は参照関係を保つ」の 1 行を追加し、同期規律を機械化の代替とする

#### C-3. CLAUDE.md §8.0 と project-memory 3 文書の相互参照欠落

- **事実**:
  - `CLAUDE.md §8.0` は DESIGN / KNOWLEDGE / QUESTIONS / ADR を「読む」4 ステップを記述するが、
    - 各文書の該当節（§0 運用プロセス、§1〜§5 知識）への具体的誘導がない
    - 「該当文書が空（エントリ 0）の場合の扱い」規約が存在しない
  - `KNOWLEDGE.md §0` / `QUESTIONS.md §0` / `adr/README.md` は独自の運用プロセスを記述するが、
    - `CLAUDE.md §8.0` のどのステップから呼ばれるかを明示していない
- **真の論点**: 「空スキャン文言の重複」ではなく、**上流 §8.0 と下流 3 文書の相互参照関係が未構築**。規約レベルでは「空の場合の扱い」自体が本テンプレートに欠落している
- **対処**:
  1. `CLAUDE.md §8.0` の各ステップに参照先文書の該当節を明記
  2. 各下流 §0 の冒頭に「本節は `CLAUDE.md §8.0` の該当ステップから呼び出される運用プロセス」の 1 行追加
  3. `CLAUDE.md §8.0` 末尾に空スキャン規約を新設（「該当文書が空の場合、空スキャンで完了とみなす。空を確認する行為自体が §1.3 の実装であり、スキップしてよい対象ではない」）

### D. ブートストラップ直後の UX 失敗

テンプレート初期状態が「健全に lint / check が通る」ようにする。方針: **Babashka は採用せず、hook / shell script で機械化を充実させる**。サンプルコードはコメント化で価値を保持する。

#### D-1. 配布時点で `clj -M:lint` が error を出す

- **現象**: `components/` と `bases/` ディレクトリ未作成で `:lint` エイリアスの `--lint "components" "bases" "development/src"` 引数が file does not exist を返す
- **対処**: `components/.gitkeep` と `bases/.gitkeep` を配布物に追加（Polylith 公式慣例と整合）。`clj -M:lint` が「0 files linted」で成功する。`:lint` エイリアス 2 段階化は不採用（構造複雑化回避）
- **着手コスト**: 極小（2 ファイル追加のみ）

#### D-2. `dev/user.clj` に Integrant / Portal セクション同梱

- **現状**: Integrant 34 行 + Portal 50 行、一部コメントアウト・一部アクティブの中途半端状態
- **対処**:
  - Malli instrumentation セクションのみアクティブ維持（必須層）
  - Integrant セクション全体を `;;` 行コメント化（IDE で複数行選択 → `;;` 除去で一括解除できる方式。`#_` は S 式単位で不向き）
  - Portal セクション全体を同様に `;;` コメント化
  - 各セクション冒頭に「採用時のコメント解除手順」を注記
- **効果**: 配布時点で `(require 'dev.user)` が評価可能（未導入依存が呼ばれない）、サンプル価値は完全保持、採用時はコメント除去のみ
- **機械化補強**: 採用時にコメント解除されれば自然に `clj -M:lint` 対象になり、依存引き込み漏れが検知される（追加設定不要）

#### D-3. リッチコメント内の `(go)` / `(reset)` / `(portal)` サンプル

- **対処**: D-2 で自動解消（`(comment ...)` 内はシンボル未解決、Integrant / Portal セクションが `;;` コメント化されていれば参照エラーなし）
- **追加措置**: リッチコメント冒頭に「Integrant / Portal セクションをコメント解除した後に使える」の注記を追加

#### D-4. brick 追加時の 4 箇所同時更新（「機械化」から「不完全さの機械検知」へ）

- **現状**: `poly create` は brick ディレクトリしか作らず、`workspace.edn :projects` / ルート `deps.edn :dev :extra-paths` / `:extra-deps` / `projects/<deploy>/resources` の 4 箇所は手動更新
- **却下案**: `scripts/new-brick.sh` で brick 追加を機械化する案は deps.edn の EDN 機械編集（sed 脆弱 / rewrite-clj は Clojure 起動コスト）で複雑化。不採用
- **採用案**: `scripts/check-brick-registration.sh` を新設し**不完全さを検知**する方向に切り替える
  - `components/` / `bases/` 配下の全 brick を走査（`.gitkeep` は除外）
  - 各 brick が deps.edn の `:dev :extra-paths` に登録されているか検査
  - 各 brick が deps.edn の `:dev :extra-deps` に `:local/root` で登録されているか検査
  - 登録漏れがあれば error 終了 + 漏れている brick 名と追加先を提示
  - 完了条件（CLAUDE.md §5.5）に F-1 経由で組み込み、brick 追加の不完全さは必ず検出される
- **参照強化**: `CLAUDE.md §8.2`（新規コンポーネント追加）→ `BOOTSTRAP_GUIDE.md §2.5` の相互参照を追加（前セッション C-3 で扱った相互参照未構築パターンの解消）

#### D-5. `clj-kondo` hook 初回取り込みの手動実行

- **現状**: `clj -M:lint --copy-configs --dependencies --lint "$(clojure -A:dev -Spath)"` をシェル展開必須で手動実行（tools.deps の `:main-opts` はシェル展開不可）。再取り込みトリガーなし
- **対処**:
  - `scripts/lint-import-hooks.sh` を新設（上記コマンドをラップ、Unix 依存は許容）
  - 運用タイミングを以下に明文化:
    - 初回セットアップ（`BOOTSTRAP_GUIDE.md §2.9`）
    - brick `deps.edn` に新ライブラリ追加時（`MAINTAINERS_GUIDE.md §5`）
    - `STACK_GUIDE.md §4.2` 推奨ライブラリ追加時
    - `clj -M:outdated` で最新化後

#### D-6. `:top-namespace "myorg.myapp"` プレースホルダ置換忘れの検知なし

- **現状**: `workspace.edn` / `deps.edn` にプレースホルダが残っても `poly check` は検知しない
- **対処**:
  - `scripts/check-placeholders.sh` を新設（grep ベース、依存なし）
  - 検査対象は**設定ファイルのみ**（`workspace.edn` / `deps.edn`）に限定。コード例（`POLYLITH_GUIDE.md` / `KNOWLEDGE.md` のサンプル中）は対象外
  - 完了条件（CLAUDE.md §5.5）に F-1 経由で組み込み


### F. 機械化候補（`scripts/` と custom hook の新規追加）

Part 1 D で導入する `scripts/` ディレクトリを統合・拡張する機械化候補群。D-4 / D-5 / D-6 と合わせて**設定ファイル・ディレクトリ構造・配布物整合性**の領分を `scripts/` が、**コード内の構文・構造パターン**の領分を `.clj-kondo/hooks/`（A 系）が担う役割分担で運用する。

#### F-1. ワークスペース整合性の総合検査 + 完了条件組み込み

- **位置づけ**: D-4 / D-6 / F-3 を統合するエンドツーエンド検査。完了条件の拡張ポイント
- **対処**: `scripts/check-workspace-integrity.sh` を新設
  - 内部で `check-brick-registration.sh`（D-4）・`check-placeholders.sh`（D-6）・`check-deprecated-libs.sh`（F-3）を呼び出す
  - 加えて以下を検査:
    - deps.edn の `:local/root` パスが実在するか
    - workspace.edn の `:projects` に列挙された project が実在するか
    - 配布時点で削除すべきファイル（`.gitkeep` と brick 併存等）の検査
- **完了条件（CLAUDE.md §5.5）への追加**: `./scripts/check-workspace-integrity.sh` を 1 行追加することで、D-4 / D-6 / F-3 の検査すべてが §5.5 の必須通過ゲートに入る

#### F-2. `dev/user.clj` セクションコメントアウト状態の整合検査（保留）

- **内容**: Integrant 採用プロジェクト（brick deps.edn に integrant 依存あり）で `dev/user.clj` の Integrant セクションがコメントアウトのままなら警告。逆（Integrant 未採用で Integrant セクションが有効）も同様
- **判定**: 優先度低。実装複雑度（brick deps.edn 走査 + `dev/user.clj` 解析）に対する価値が未検証。D-2 で `;;` コメントアウト配布を導入した直後は運用観察で十分
- **対処**: 本セッションでは保留し、将来の実装候補として記録のみ

#### F-3. STACK_GUIDE.md §8.2 非推奨ライブラリの brick deps.edn 検査

- **位置づけ**: A-6 の clj-kondo `:discouraged-var`（コード内使用検知）を補完する、`deps.edn` 採用宣言自体の検査
- **対処**: `scripts/check-deprecated-libs.sh` を新設
  - 各 brick の `deps.edn` が非推奨ライブラリ（timbre / Compojure / friend / clj-http 新規 / data.json 等）を含んでいないか grep で検査
  - 登録があれば error 終了 + 推奨代替を提示
- **A-6 との役割分担**:
  - **A-6（clj-kondo）**: コード内で `timbre/info` 等の**関数呼び出し**を検知
  - **F-3（shell script）**: `deps.edn` での `com.taoensso/timbre` 等の**採用宣言**を検知
  - 両者で二重に防御。非推奨ライブラリが見逃されない

#### F-4. Malli instrumentation 有効検査（保留）

- **内容**: REPL 起動時に `malli-on!` が呼ばれたかを検査する動的テスト
- **判定**: REPL 内の動的検査でビルド時検査ではないため、本計画の機械化戦略（静的検査充実）と性質が異なる。優先度低
- **対処**: 本セッションでは保留し、将来の実装候補として記録のみ

### G. 運用姿勢の昇格候補（KNOWLEDGE 昇格候補）

本セッションの議論から抽出された、本テンプレートの設計哲学・運用姿勢として明文化すべき 3 件。ユーザー合意の上で `CLAUDE.md` / `MAINTAINERS_GUIDE.md` / `KNOWLEDGE.md` に昇格させる候補。

#### G-1. 機械化充実を運用姿勢として明文化

- **観察**: 本テンプレートの §1.2.1 機械化原則は「規約をツールで強制する」と述べるが、**機械化をどこまで充実させるか**の運用姿勢は従来不明だった
- **方針（ユーザー明示）**: 「機械的なチェックはできるだけ自動化し、充実しているほうが良い」
- **帰結**: 新しい機械化候補を発見した時の判断基準が「実装可能なら採用、コスト高すぎる場合のみ保留」に変わる。従来の「実装コストとトレードオフで判断」より機械化寄りに軸足を置く
- **記録先**: `CLAUDE.md §1.2.1` 末尾に運用姿勢として追記、または `MAINTAINERS_GUIDE.md §5`（機械化戦略）に記述

#### G-2. hook と script の役割分担

- **hook の領分**（`.clj-kondo/hooks/` 配下、Clojure コード解析）:
  - 関数行数（A-7）、`m/=>` 付与（A-1）、`Exception.` 禁止（A-4）、非推奨ライブラリの**コード使用**（A-6）、catch Throwable 禁止（A-10）、空 catch（A-11）、ドメイン層 I/O（A-12）等
- **script の領分**（`scripts/` 配下、shell）:
  - プレースホルダ（D-6）、brick 登録（D-4）、非推奨ライブラリの**deps.edn 採用宣言**（F-3）、`dev/user.clj` セクション状態（F-2 保留）、ワークスペース整合性（F-1）等
- **理由**: clj-kondo は Clojure コード AST 解析が得意で設定ファイル構造の検査は苦手。shell script は逆。役割分担で両者の強みを活かし、重複開発を防ぐ
- **記録先**: `MAINTAINERS_GUIDE.md §5` に「機械化戦略の実装手段」として追記

#### G-3. 「前提の明示」による LLM 提案解空間の収束

- **観察**: D 群対処検討で、ユーザーが「サンプル価値あり、bb 不採用、shell script 優先」と前提を早期に明示したことで、LLM の提案解空間（Babashka 導入、条件付き生成、2 系統配布等の複雑な方向）が閉じられ、単純な方向（`.gitkeep`、`;;` コメント化、shell script）に収束した
- **一般化**: LLM 駆動開発では、ユーザーが価値判断・運用制約を早期に明示するほど設計コストが下がる。§1.3「生きた知識の活用で再発見の疲労を避ける」の具体例の一つ
- **記録先**: `CLAUDE.md §1.3` の実例として追記、または `KNOWLEDGE.md` 新節「LLM 協働の原則」等に記録

---

## Part 2. 無視対象（原理誤読、または実体なし）

以下は「指摘として立派に見えるが、CLAUDE.md §1 / §1.2.5 / §1.3 に照らすと誤読・過剰要求」に該当。列挙するが対処は不要。

### F. 「検証不能」を一律問題視する指摘（原理誤読）

CLAUDE.md §1.2.5 は「承認・記録の付加は不可逆性の根拠があって初めて正当化」と明言。可逆・修復可能な判断に検証機構を要求するのは原理違反。

**無視**:

- LLM の自己申告（§8.0 実装前確認、§8.1(7) KNOWLEDGE 昇格提案、§11.3 5 役割実施率）
- ONE BY ONE 原則例外判定の主観性（COLLABORATION_GUIDE.md §4.3）
- 自己停止プロトコルのカウント主観性（CLAUDE.md §7.1）
- Q を立てるべき場面 15 項目の遵守検証（QUESTIONS.md §1）
- ADR アンチパターンの判定主観性
- STACK 各 stack の「採用時確認 N 項目」の機械化要求
- タスク受領時の事前チェック（§7.4）の記録化要求
- `[ASSUMPTION-N]` マーク追跡の要求
- 段階的承認チェックポイント 4 種の適用判定
- 曖昧性典型 6 パターンの発見漏れ検知
- 協働アンチパターン 6 種の検知機構

**理由**: これらは本質的に L2 / L3 の判断で、判断が可逆だから承認不要の運用になっている。検証機構を付け足すと§1.2.5 に反する儀式追加になる。機械化できる部分だけ A 節に昇格させ、それ以外は「LLM 判断が揺れたら都度修復」で済ませる

### G. 「重複」を一律問題視する指摘（役割分離の誤読）

§11 は「4 種文書は役割対照」と宣言。人間向け文書と LLM 向け文書も役割が違う。同じ情報の二重管理ではなく、用途別の再表記。

**無視**:

- README.md §各文書への導線 × CLAUDE.md §本文書群の参照関係（対象読者違い）
- README.md §設計の基底思想 × CLAUDE.md §1（要約と本体、対象読者違い）
- COLLABORATION_GUIDE.md §8 の関連文書表（末尾索引、本文参照とは別用途）
- BOOTSTRAP_GUIDE.md §1 前提 × CLAUDE.md §3 × STACK_GUIDE.md §2 の一部重複
  - ただし C-2 で「STACK_GUIDE.md を正本化、他は参照のみに縮退」を選ぶなら有効問題。ここでの「無視」は「3 箇所に独立して書き込まれていること自体は原理違反ではない」の意

**理由**: §11 原則 13「仕様・知識・決定履歴・判断保留の分離」は文書間の内容重複をそもそも前提としている。重複検出を機械化するのではなく、「どれが正本か」を宣言する設計で十分

### H. 未読領域からの推測項目（実体なし）

過去セッションで「精読していないが潜在的にある」と列挙された項目。事実調査が完了していない。

**無視**:

- MAINTAINERS_GUIDE.md §1-§10 の全原則の「派生プロジェクトへの染み出し」リスト（原則 239-250）
- STACK_GUIDE.md §4.2.4-§4.2.10 の「各 stack 推奨・避けるべき・採用時確認」の網羅列挙（311-354）
- adr/template.md、DESIGN.md §0、KNOWLEDGE.md §1-§5 などの「未精読」ラベル項目

**理由**: これらは実際に中身を読まずに「網羅できていない」と宣言しただけで、問題の実体が確認できていない。CLAUDE.md §1.2.3 小単位分解の原則から、問題を拡散させずに「実際に読んで確認した項目だけ」を Part 1 に昇格させる運用にする

### I. 遅延投資型の項目（現在は問題ではない）

将来価値を生むが現時点では空回りの投資。§1.2.4 早期破棄の判断軸では「今は手を入れない」が正解。

**無視**:

- 初回 stable タグ付与（ブートストラップ直後は brick 1 個で意味がない）
- `clj -M:outdated` の定期実行機構（プロジェクトが稼働してから必要）
- `clj -M:poly info` / `clj -M:poly deps` の利用トリガー
- `workspace.edn :compact-views` / `:default-profile-name` の用途説明
- 禁止ライブラリ 20 種の ADR 列埋め（使うなら理由付けだが、使わないなら不要）

**理由**: 現時点で発生していない疲労を先回りで解消しようとすると、§1.2.5「念のため」原則違反になる。実プロジェクトで稼働してから該当ケースに当たれば対処する

### J. 本セッション運用由来の雑事（テンプレート本体の問題ではない）

**無視**（または別ファイルで追跡）:

- `/home/taka/.claude/plans/` のプラン累積
- codex レビュー転写の累積（500KB+）
- `.gitignore~` / `AGENT.md~` バックアップ
- git 分岐（local 6 vs remote 2）— 運用上の issue で、テンプレート設計の問題ではない
- 本セッションで生成された memory ディレクトリ等

**理由**: 本ファイル `_POSSIBLE_ISSUES.md` はテンプレート品質の指摘集であり、個別セッション運用の雑事は別軌道で処理する

### K. 「教育的冗長性」を削ろうとする指摘（対象読者無視）

adr/README.md の ADR 解説 158 行、CODING_GUIDE.md §1 メタ解説、「迷ったら 3 つ」等の格言表現など。

**無視**:

- 派生プロジェクトの LLM がこれらを読むコストは確かにあるが、人間側の学習コンテンツとして機能している
- 削除するより「LLM 向けに読む必要がある節」と「人間向け参考節」を章立てで分けるのが筋だが、本質的問題ではない

### L. 語面類似を一律「ゆれ」と見る指摘（意味論未精査）

元 C-4 の用語統一指摘を格下げした。語面が似ているからといって一律「ゆれ」と判定するのは、各語の使用文脈・抽象レベルを精査していない原理誤読。

**無視**:

- 「早期破棄」（CLAUDE.md §1.2.4 の原則名）と「撤退」（§7.3 の実装プロトコル名）は**抽象レベルが異なる並立語**で、ゆれではない
- 「早期ピボット」は `CLAUDE.md:111` で §1.2.4 と §1.2.5 を統合する「思想」として 1 回使用されている乱用なしの統合概念
- 「承認」（裁可）/「確認」（検証）/「合意」（最終段階）は**意味論的に区別される別語**で、統一すると情報が失われる

**理由**: 用語統一は機械的な語面一致判定では済まない。各語の使用文脈を精査して初めて「ゆれ」か「意図的並立」かを判定できる。語面類似だけで「ゆれ」認定するのは Part 2 F「検証不能を一律問題視」と同型の原理誤読。

---

## 3. 優先順位と次アクション

レビュー反映後の判定。

| 優先度 | 対象 | 根拠 | 着手コスト |
|---|---|---|---|
| **高** | A-2（require sort を :error へ） | 1 行設定変更で §1.3 違反 1 件解消 | 低 |
| **高** | A-4（`Exception.` を :discouraged-var 登録） | 1 行設定追加 | 低 |
| **高** | A-5（with-redefs 規約を DI+ADR に変更） | 主観語を検査可能運用に置換 | 低（規約改訂） |
| **高** | B-2（supersede チェイン片方向化） | 双方向更新の儀式撤廃 | 低（規約改訂） |
| **高** | B-4（`malli-off!` 削除） | §1.2.5 違反の典型解消 | 低 |
| **高** | D-1（`components/.gitkeep` と `bases/.gitkeep` 追加） | 配布時点 lint 失敗の解消 | 極小 |
| **高** | E-2（本プラン完了条件未達） | §5.5 違反をそのまま残している | 低 |
| **高** | A-1（m/=> 契約 custom hook） | **§1.1.1 全域性の守護機構**、最優先 | 中（hook 1 本） |
| **中** | A-3（CODING §1.9 見直し、:skip-comments は維持） | 元規約の読み違え解消 | 低（規約削除） |
| **中** | A-6（非推奨ライブラリ clj-kondo 登録） | §1.3 機械化強化 | 中（シンボル登録） |
| **中** | A-7（関数行数 clj-kondo hook） | ターン内検証に乗る機械化 | 中（hook 1 本） |
| **中** | B-1（§7.2 を「必要時のみ」に緩和） | 毎ターン儀式の軽量化 | 低（規約改訂） |
| **中** | B-3（resolved Q 選別削除） | ADR 昇格 Q のみ削除、他は軽く残す | 低（規約改訂） |
| **高** | D-2（Integrant / Portal セクションを `;;` でコメントアウト + 解除手順明記） | 配布時点 REPL 評価可能化、サンプル価値保持 | 小〜中（`dev/user.clj` 編集） |
| **中** | D-4 対処（`scripts/check-brick-registration.sh` 新設 + 相互参照強化） | brick 登録漏れの機械検知 | 小（shell + 文書） |
| **中** | D-5（`scripts/lint-import-hooks.sh` + 運用タイミング明文化） | hook 再取り込みの自動化 | 小 |
| **高** | D-6（`scripts/check-placeholders.sh` + 完了条件組み込み） | 置換忘れの機械検知 | 小 |
| **中** | F-1（`scripts/check-workspace-integrity.sh` 統合 + §5.5 組み込み） | 完了条件に総合検査を 1 行追加 | 小 |
| **中** | F-3（`scripts/check-deprecated-libs.sh` 新設） | A-6 を deps.edn 側で補完 | 小 |
| **低** | F-2（`dev/user.clj` セクション整合検査） | 保留、実装複雑度高 | 中 |
| **低** | F-4（Malli instrumentation 動的検査） | 保留、本計画の静的検査戦略と異なる | 中 |
| — | G-1 / G-2 / G-3（運用姿勢・役割分担・前提明示の KNOWLEDGE 昇格） | 別途ユーザー合意で昇格 | 小（文書追記） |
| **高** | A-8（m/=> と defn アリティ整合 hook） | Malli 契約整合性、本テンプレ核 | 中（hook 1 本） |
| **高** | A-9（ドメイン top-level atom 禁止） | §1.1.2 不変性守護 | 低（:config-in-ns 活用） |
| **高** | A-10（catch Throwable / Exception 禁止） | §7.2 catch 限定化 | 中（hook 1 本） |
| **高** | A-11（空 catch 禁止） | §7.2 握り潰し防止 | 低（hook 1 本） |
| **高** | A-12（ドメイン層 slurp/spit 禁止） | §1.1.3 副作用隔離、雛形有効化のみ | 低 |
| **低** | E-1（本プラン ADR 発行） | 決定履歴保全 | 中（ADR 1 本） |
| **中** | C-2（STACK_GUIDE を正本と明示 + MAINTAINERS に同期規律追加） | version drift 防止、日常参照コスト維持 | 中（2 文書編集） |
| **中** | C-3（CLAUDE.md §8.0 と下流 3 文書の相互参照構築、空スキャン規約新設） | 規約レベルの欠落是正 | 中（4 文書編集） |
| — | C-4 は Part 2 L に格下げ | 原理誤読 | — |
| — | D-3（D-2 で自動解消、冒頭注記のみ追加） | D-2 の副産物 | 極小 |
| **中** | A-13（m/validate 戻り値捨て検知） | 初心者ミス検出 | 中（hook 1 本） |
| **中** | A-14（1 ファイル 1 ns） | false positive なし | 低（hook 1 本） |
| **中** | A-15（位置引数 4 個以上） | §4.2 機械化 | 低（hook 1 本） |
| **中** | A-16（go 内 `<!!`/`>!!` 禁止） | §10.2 機械化 | 低（hook 1 本） |
| **中** | A-17（destructuring ネスト 2 超過） | §1.12 機械化 | 中（hook 1 本） |
| **中** | A-18（production Thread/sleep 禁止） | 副作用隔離 | 低（:discouraged-var） |
| — | A-19 | 保留・除外項目（詳細は本文） | — |

段階分けは以下の通り。本ファイル再記述（段階 0）は既に完了、段階 1 以降が実装タスク。

**段階 1**（完了条件に関わる最小セット、半日以内）:
D-1（`.gitkeep`）・D-6（`check-placeholders.sh` + §5.5 組み込み）。2 ファイル追加 + 1 script + §5.5 の 1 行変更で完了。

**段階 2**（ブートストラップ体験の改善 + shell script 充実、1〜2 日）:
D-2（`;;` コメント化）・D-3（注記）・D-5（`lint-import-hooks.sh`）・D-4 対処（`check-brick-registration.sh`）・F-1（`check-workspace-integrity.sh` 統合）・F-3（`check-deprecated-libs.sh`）・E-2（前セッション完了条件実行）。`scripts/` ディレクトリ 5 本の新設と `dev/user.clj` の書き換え、関連文書への相互参照追加

**段階 3**（即採用可能な設定変更・規約緩和、1 日以内）:
A-2・A-4・A-5・A-9・A-11・A-12・A-14・A-15・A-16・A-18・B-2・B-4。規約改訂・`:discouraged-var` 追加・既存 `:config-in-ns` 雛形の有効化・単純 hook のみで完了

**段階 4**（複合 custom hook 実装、2〜3 日）:
A-1・A-7・A-8・A-10・A-13・A-17。`.clj-kondo/hooks/` 配下に custom hook を追加。**A-1 は §1.1.1 全域性、A-8 は Malli 契約整合、A-10 / A-11 は §7.2、A-12 は §1.1.3 で、三基底原則すべてを機械化で守る束**

**段階 5**（文書整合・運用姿勢の昇格、随時）:
A-6（非推奨ライブラリ登録）・B-1 / B-3 / A-3（規約緩和）・C-2 / C-3（正本化と相互参照）・E-1（本プラン ADR 発行）・G-1 / G-2 / G-3（運用姿勢の KNOWLEDGE / `CLAUDE.md` / `MAINTAINERS_GUIDE.md` 昇格）

---

## 4. 後続タスク: 相互参照構造の体系的点検

C-3 で観察された「上流で定義すべき規約が下流に散逸し、相互参照が未構築」というパターンは、他の文書境界にも存在する可能性が高い。本ファイルのスコープでは個別対処に留め、別計画で体系的点検を行う。

**点検対象候補**（未調査、次タスク着手時に要精査）:

- `CLAUDE.md §8` 作業プロトコルと `POLYLITH_GUIDE.md` / `CODING_GUIDE.md` の相当節の相互参照
- `CLAUDE.md §6` Polylith / stack 運用と `STACK_GUIDE.md` の相互参照
- `CLAUDE.md §11.1` 4 種文書概観と 各文書 §0 運用プロセスの相互参照
- `COLLABORATION_GUIDE.md` 各モードと `CLAUDE.md §8.x` 作業プロトコルの相互参照

**判定軸**（C-3 の経験から抽出）:

- 上流文書で「どの下流文書のどの節から呼ばれるか」を明示しているか
- 下流文書で「上流のどのステップから呼び出されるか」を明示しているか
- 「空の場合」「例外ケース」の扱いを上下どちらかに 1 回だけ記述しているか
- 参照が双方向に張られているか（片方向欠落で LLM の情報探索が詰まる）

---

## 5. 本ファイル自体の扱い

本ファイルは一度性の論点集であり、Part 1 の項目を実装に移し終えたら削除してよい。ADR に転記する項目があれば転記し、それ以外は git log が軌跡を保全する。§1.2.5 に照らせば、このファイルを長期保管する必要はない。
